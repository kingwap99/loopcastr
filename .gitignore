# ── Secrets: never committed ───────────────────────────────────────
# The stream key, Telegram token, YouTube OAuth token and cookies stay on the machine only.
stream.key
telegram.json
youtube-oauth.json
cookies.txt
alert_webhook
webui-token
*.pem
work/playout/auto.key
work/playout/auto.crt

# ── Media files: large and reproducible ────────────────────────────
*.mp4
*.mkv
*.mov
*.ts
*.m4a
*.mp3
*.png
*.jpg

# ── Runtime artifacts ──────────────────────────────────────────────
logs/
*.log
*.jsonl
*.out
*.err
*.bak
__pycache__/
*.pyc
tmp/
.DS_Store

# Reproducible content and lists (keeps multi-GB media out of commits)
media/
media-test/
playlist-local.json
playlist-local-test.json
playlist.json
stream.json
concat.txt
concat-test.txt

# ── Intermediate artifacts and old experiments (production content is in the repo root) ──
work/
